# 2026-08-07.14 — fold Q-0001/Q-0002 into decisions.md as D10/D11

Kind: mech
Task: none (documentation leg named by the board's own Blocked section)
Commit: (this one)

## What changed

`vault/feedback/` held nothing but its README, so per CLAUDE.md's pick order
the leg fell through to the board. The board's `Awaiting review:` is
**PARSE-02**, so only `[mech]` work was eligible. The board's own Blocked
section already named the leg: fold the answered Q-0001 and Q-0002 into the
knowledge docs and delete the question files — the same thing the previous
leg (2026-08-07.13) noted was correctly out of scope for it and left for
"its own leg."

Read both question files in full, not just the board's summary, before
writing anything down.

**Q-0001 (file or tree)** became **D10** in `decisions.md`: `margin` with no
args opens a tree of the working directory, `margin DIR/` a tree of that
directory, `margin FILE.md` is unchanged; the tree pane lists markdown files
only, pruning directories with none beneath them; `cmd/margin` moves from
`cobra.ExactArgs(1)` to `cobra.MaximumNArgs(1)`; thread storage needs no
format change since D9 already keys by document path. I put this in
`decisions.md` rather than `interaction.md` because the answer is a written
decision, not something judged on a screen — `interaction.md`'s own preamble
restricts it to behaviour "judged by the maintainer on a real screen." The
pane's actual appearance is still exactly that, and stays open.

**Q-0002 (resolution and deletion)** became **D11**, explicitly extending D9
rather than replacing it (D9's own text already anticipated this split):
resolution is one boolean, clearable by either the reviewer or an agent —
not the two-state `addressed`/`resolved` split that was on the table; and
deletion is a tombstone, keeping author and timestamp, dropping the body so a
reply that answered it doesn't dangle.

Then updated everything that pointed at the two questions so nothing is left
referencing a deleted file:

- `interaction.md`'s "Not settled" list: replaced the generic "what the
  tree view looks like" bullet with the specific open sub-questions D10's
  answer left felt (pane appearance/position/toggle/focus, cross-tree
  progress, export scope), and added a new bullet for D11's leftover felt
  questions (whether a tombstone renders, whether resolved threads
  collapse/dim/disappear, which keys).
- `board.md`: THREAD-01 and THREAD-03 no longer say "Blocked on Q-0002" —
  they cite D11 for the settled shape and are now genuinely pickable
  `[mech]` work, not just theoretically unblocked. The M3 navigation
  section's "Not blocked by Q-0001" framing (which referenced a file that no
  longer exists) became a reference to D10. The Blocked section itself now
  just says "(nothing)" plus a pointer to D10/D11, replacing the interim
  summary a previous leg had left there as a reading aid.
- `decisions.md`'s existing D9 text referenced "Q-0001" and "Q-0002" by name
  for the future-tree and resolution/deletion caveats; repointed both to
  D10/D11 now that those are the citable, permanent entries.

Deleted both question files last, once everything that referenced them had
somewhere else to point.

## What this deliberately does not do

No code. `cmd/margin`'s `MaximumNArgs(1)` change, the tree pane, the
resolved-flag and tombstone fields on the thread frontmatter — none of that
is built here. This leg only makes the two decisions citable and durable;
implementing them is separate mechanical work (THREAD-01/THREAD-03 for D11;
whatever M3 leg picks up D10) still ahead on the board.

## Verification

Documentation-only change. `make check` run anyway per the instructions to
confirm the tree is still green: build, tests, and vet all pass unmodified.

No question raised. Board's `Awaiting review:` stays on **PARSE-02** — this
leg did not touch it.

## Note on commit authorship

D8 in `decisions.md` settles that commits stay authored by the maintainer
with `Co-Authored-By` disclosure, and that a stop hook asking to amend that
should be declined. Noting this per D8's own instruction in case the hook
fires on this commit.

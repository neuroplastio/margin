# 2026-08-11.8 — close M3, open M4 on the roadmap and board

Kind: mech
Task: The board's `Backlog — M3` sections held only stale lines and
`vault/feedback/` and `vault/questions/` held only their READMEs, but the
roadmap's M3 "Navigation between documents" item was still `- [ ]` — the
milestone-transition bookkeeping was never done even though every M3 item
landed (journals 2026-08-11.1–.4). This leg records the transition, mirroring
2026-08-10.24's M2→M3 close.
Commit: (this one)

## What changed

No code. The roadmap, board and the record of what shipped disagreed, and with
M3's backlog lines all struck the next leg had nothing to pick in milestone
order. Three edits bring the documents in line with the Done section:

- **Roadmap M3 → done.** The "Navigation between documents, and a
  cross-document comment inbox" item goes `- [ ]` → `- [x]`: both halves
  landed 2026-08-11 — cross-document link following (journal 2026-08-11.2)
  and the comment inbox (journal 2026-08-11.3). A `Status: done, 2026-08-11`
  line and a note that the milestone's felt surface (tree pane, inbox,
  link-following, progress readouts) is still on the board's `Needs a look:`
  list — that list is the log of the milestone's unjudged felt work, so the
  close records it rather than pretending it is settled.
- **Roadmap M4 → current.** `Status: current, 2026-08-11.` The three items
  stay `- [ ]` — they are real legs, not bookkeeping.
- **Board.** `Active milestone` moves to M4; `Last updated` notes the close.
  The `Backlog — M3 (tree view)` section's four items are all struck with a
  "closed 2026-08-11" note (the last one, tree-wide review progress, was
  already done via journal 2026-08-11.4 but its backlog line was never
  cleared). A new `Backlog — M4 (round two)` section is seeded from the
  roadmap — the three items plus the exit criterion — with a note that the
  first item (how a previous revision is recorded and located) touches
  on-disk format, so expect a question or a Dn before building it.

**What I chose and rejected:** a docs-only transition leg rather than folding
the close into the first M4 build (a leg is one logical change, and the
transition is that change — it is what makes "take the next unblocked board
item in milestone order" well-defined again, exactly as 2026-08-10.24 argued);
leaving M3's `- [ ]` unchecked-with-an-explainer rather than ticking it (the
explainer was itself the leftover — the item had landed and the note knew it);
adding the M4 on-disk-format warning to the board so the next leg does not
wander into a format decision unawares.

## Notes

- M3's done date is 2026-08-11 (when the last tree item, review progress,
  landed via journal 2026-08-11.4; the navigation item's felt halves landed
  the same day).
- The "open question that gates the design" paragraph in the roadmap's M3
  section is left as-is: it is already annotated as answered (Q-0001 → D10)
  and records the rationale, which is its job.
- No on-disk format, anchor or interaction behaviour is touched — this is
  purely the roadmap/board documents.

## Verification

`make check` green (build, test, vet) — nothing in this leg compiles; the gate
confirms the tree stays green.

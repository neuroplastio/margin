# 2026-08-10.24 — close M2, open M3 on the roadmap and board

Kind: mech
Task: The board's backlogs were all empty and `vault/feedback/` and
`vault/questions/` held only their READMEs, but the roadmap's M1/M2 items were
still `- [ ]` — the milestone-transition bookkeeping was never done even though
the board's Done section shows every item and exit criterion landed. This leg
records the transition.
Commit: (this one)

## What changed

No code. The roadmap and the board disagreed with the record of what has
actually shipped, and the board being empty of backlog items meant the next leg
had nothing to pick in milestone order. Three edits bring the documents in line
with the Done section:

- **Roadmap M1 → done.** All five items landed (PARSE-01/02, RENDER-01..07,
  ID-01/02). The lazy-stamping item gets an honest note: the *wiring* is a
  known gap recorded in D12 (`stampAll` exists and is tested, nothing in the
  running app calls it), so "done" here means the infrastructure, not the live
  call path — the exit criterion "a comment left today is still attached
  tomorrow" holds only while block text is unchanged, which D12 already says.
- **Roadmap M2 → done.** All seven items landed across STORE-01/02/03,
  EXPORT-01..05, THREAD-01..04, ID-02, D13–D15. Exit criterion met: the loop
  (export → agent revises + replies in thread files → reopen and see replies →
  see resolved threads) is the `margin skill`/`export`/`comment add`/`comments
  wait` story, complete since 2026-08-10.12.
- **Roadmap M3 → current.** The three scroll items that already shipped
  (SCROLL-01, SCROLL-02, SCROLL-03) are `[x]`; the remaining four stay open. The
  "open question that gates the design" line is annotated as answered — Q-0001
  closed as D10 (unit of review is both a file and a tree, chosen by argv), and
  the note now points at interaction.md's "Not settled" for the still-felt parts
  (the tree pane's look/position/toggle/focus).
- **Board.** `Active milestone` moves to M3; `Last updated` notes the close.
  The M3 backlog is seeded from the roadmap — a new `Backlog — M3 (tree view)`
  section for the tree work (review a directory, cross-document navigation +
  inbox, tree-wide progress) and the existing `Backlog — M3 (navigation)`
  section for the tree-independent single-document item (link navigation with a
  jumplist), with a note that SCROLL-01/02/03 already landed.

**What I chose and rejected:** closing the milestone in a docs-only leg rather
than folding it into the first M3 build (a leg is one logical change, and the
transition is that change — it is what makes "take the next unblocked board
item in milestone order" well-defined again); marking the lazy-stamping item
done-with-a-gap rather than leaving it open (the gap is real but recorded, and
leaving M1 open would wrongly block M3; the D12 pointer preserves it for
whoever wires stamping into a live command).

## Notes

- The M1 "done" date is 2026-08-08 (when RENDER-07 emptied `Backlog — M1`);
  M2's is 2026-08-10 (the agent-loop legs that finished the exit loop).
- No on-disk format, anchor or interaction behaviour is touched — this is
  purely the roadmap/board documents.

## Verification

`make check` green (build, test, vet) — nothing in this leg compiles; the gate
confirms the tree stays green.

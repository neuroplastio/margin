# 2026-08-07.13 — drain feedback: frontmatter-vanishes is RENDER-07, not a new item

Kind: mech
Task: none (feedback drain — see `vault/feedback/README.md`)
Commit: (this one)

## What changed

`vault/feedback/2026-08-07-frontmatter-vanishes.md` was the oldest (only) item
in the inbox, so per CLAUDE.md's pick order it preempted the board. Read in
full before doing anything else, because the board's `Awaiting review:` is
currently **PARSE-02** — a felt leg is already parked awaiting a human, and
CLAUDE.md is explicit that at most one may be outstanding at a time. The
feedback's own content is a felt rendering decision (how frontmatter should
look), so building it now would have broken that rule regardless of how the
gate reads.

Checked whether it was already covered rather than assuming: it was.
`vault/journal/2026-08-07.5.md` (PARSE-03, commit `a5de348`) filed **RENDER-07**
for exactly this the moment it landed — its board text already names the same
three candidates (hidden-on-demand, dimmed key/value block, collapsed
line) this feedback restates. The feedback's own "Process note" section is
also already handled: it names the failure mode ("does something now render
differently, or not render at all?") that the *same commit which filed this
feedback* (`b4f44cb`) had already written into CLAUDE.md's felt/mechanical
section, verbatim. So nothing in the feedback was new decision content — it
was the maintainer confirming the gap and handing over a concrete fixture.

**What this leg actually did**, since "already covered, delete the file" on
its own would throw away the one piece of new value in it: folded the
feedback's worked example (a `retry-policy` frontmatter block with
`status`/`description`/`tags`) into RENDER-07's board entry as the suggested
demo fixture, so whoever picks RENDER-07 up has a realistic key/value spread
to render against instead of inventing one. Then deleted the feedback file,
per the inbox's own rule that an addressed item is deleted in the leg that
addresses it — the permanent record is this entry plus the board's git
history (`vault/feedback/README.md` documents how to recover the exact
wording via `git log --diff-filter=D`).

Also checked `vault/questions/`: both Q-0001 and Q-0002 are already answered
and the board's Blocked section already reflects that, but the question files
themselves are still present unfolded into `interaction.md`/`decisions.md`.
Left them alone — the feedback item was the higher-precedence pick this leg,
and folding two separate settled questions into the knowledge docs correctly
is its own leg, not a rider on this one.

## What this deliberately does not do

No rendering code, no new branch in `render()`, nothing that decides frontmatter's
visual treatment. That stays exactly as felt and exactly as blocked on the
maintainer as it was before this leg — RENDER-07 is unstarted, still in the M1
backlog, still gated behind `PARSE-02`'s review.

## Verification

Docs-only change (board + journal + feedback deletion). `make check` run
anyway to confirm the tree is still green with nothing code-side disturbed.

No question raised. Board's `Awaiting review:` stays on **PARSE-02** — this
leg did not touch it.

## Note on commit authorship

D8 in `decisions.md` settles that commits stay authored by the maintainer
with `Co-Authored-By` disclosure, and that a stop hook asking to amend that
should be declined. Noting this per D8's own instruction in case the hook
fires on this commit.

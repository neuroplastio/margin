Feature request: import comments from the current GitHub pull request.

When opening a margin doc, the user can use an import action to load comments
from the current PR. Integration via the `gh` CLI for now. Rely on `gh` CLI
detecting the PR to get the comments from.
